---
description: Boost note importance (weight) — surfaces it first in next recall
argument-hint: "<note-id> [<weight>]"
---
Parse `$ARGUMENTS` as `<id> [<weight>]`. Default weight is `1.5` if omitted. Run `memctl weight <id> <weight>` and confirm the new weight.
